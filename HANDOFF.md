# WA-Chain サイト 引き継ぎ資料

このサイトの編集を引き継ぐ方へ。**最初にこの文書を読んでください。**

---

## 1. これは何か

和紙と文化財修復のオンラインスクール「WA-Chain」のウェブサイトです。

React + Vite で作られた静的サイトで、サーバーもデータベースも（ほぼ）ありません。
ビルドすると `dist/` に HTML と画像が出力され、それをそのまま配信しています。

**入口が2つあります。同じソースコードから2つのページが作られます。**

| URL | 中身 | 元ファイル |
|---|---|---|
| `/` | 講座・和紙マップ・活動報告を含む全体 | `index.html` |
| `/about/` | ミッション・活動実績・メンバーのみ（チーム紹介用） | `about/index.html` |

`/about/` は、講座を見せずにチーム紹介だけを共有したいときのための独立版です。
中身は同じ `src/pages/AboutPage.jsx` を使っているので、**Aboutの文章を直すと両方に反映されます。**

---

## 2. 動かし方

必要なもの: **Node.js 18以上**（https://nodejs.org からインストール）

```bash
npm install
```

```bash
npm run dev
```

表示された `http://localhost:5173` などをブラウザで開くと、編集内容がすぐ反映されます。

公開用のビルド:

```bash
npm run build
```

---

## 3. どこを編集すればいいか

**ほとんどの編集は、下の表のファイルを直すだけで終わります。**
`src/components/` や `src/pages/` の中の見た目を作っているコードは、
文章を直すだけなら触る必要はありません。

| 直したいもの | ファイル |
|---|---|
| サイト上の文章ほぼ全て（日本語・英語） | `src/i18n/strings.js` |
| 講義の中身（本文・クイズ） | `data/lessons.json` |
| 活動報告の記事 | `data/news.json` |
| 和紙マップの内容 | `data/washiPapers.json` |
| 用語辞典 | `data/glossary.json` |
| トップの大きな写真 | `data/heroImages.json` |
| 動画の埋め込み | `data/media.json` |
| 写真ファイルそのもの | `public/images/hero/` に置く |
| チームのSNSアカウント | `src/config/social.js` |

### 文章を直すときの注意

`src/i18n/strings.js` は日本語と英語が**別々のブロック**に分かれています。
片方だけ直すと、もう片方の言語で古い文章が残ります。**必ず両方直してください。**

ファイル内を検索するときは、キー名（例: `aboutMission`）で探すと両方が見つかります。

---

## 4. 公開（デプロイ）の仕組み

**Cloudflare Pages** で配信しています。

現在の想定では、GitHub にプッシュすると Cloudflare が自動でビルドして公開します。
GitHub が使えない場合は、手元でビルドしたものを直接アップロードできます:

```bash
npm run build
```

そのうえで Cloudflare Pages の管理画面 → 対象プロジェクト → **Create deployment** →
`dist` フォルダをドラッグ＆ドロップ。

> **重要**: `dist/` は毎回作り直されるフォルダです。ここを直接編集しても意味がありません。
> 必ず元ファイル（`src/` `data/`）を直してからビルドしてください。

---

## 5. 触るときに気をつけること

### 写真は必ず圧縮してから入れる

カメラの元データは1枚10〜20MBあります。そのまま `public/` に入れると、
**そのファイルがそのまま公開先にアップロードされ、表示も遅くなります。**

目安: 長辺1600px前後、JPEG品質80、1枚300KB以下。

### `dist/` `node_modules/` は編集しない

どちらも自動生成されるフォルダです。

### デザインの基本設定

色・余白・フォントは `src/tokens.css` の先頭でまとめて定義しています。
個別のページで色を直接指定せず、ここの変数を使ってください。

### 変更したら必ず確認する

```bash
npm run build
```

エラーが出たら、その内容を読んでから直してください。ビルドが通らない状態で
公開すると、サイトが更新されません。

---

## 6. まだ終わっていない作業

`input/今後追加したい機能メモ.txt` に、依頼と対応履歴が残っています。
【未実装】の欄が、これから手をつける項目です。

そのほか、把握している課題:

- **`public/images/hero/` に未使用の写真が大量にあります**（約185MB・69ファイル）。
  どこからも参照されていない撮影元データです。整理すると公開が速くなります。
- **`README.md` の記述が古いままです。** デプロイ先を Vercel / GitHub Pages と
  書いていますが実際は Cloudflare Pages で、`/about/` の説明もありません。
  この `HANDOFF.md` の方が新しい情報です。
- **講義に出典・監修の表示がありません。** 専門家向けの教材なので、
  参考文献の明示が必要です。

---

## 7. 連絡先

WA-Chain: wachain2026@gmail.com
